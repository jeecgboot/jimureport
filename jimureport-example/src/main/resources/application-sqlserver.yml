server:
  port: 8085
spring:
  #配置静态资源
  mvc:
    static-path-pattern: /**
  resource:
    static-locations: classpath:/static/
  #配置数据库
  # Note: encrypt=true enables SSL/TLS, trustServerCertificate=true bypasses certificate validation
  # For production, consider using trustServerCertificate=false with proper SSL certificates
  datasource:
    url: jdbc:sqlserver://127.0.0.1:1433;SelectMethod=cursor;DatabaseName=jimureport;encrypt=true;trustServerCertificate=true
    username: sa
    password: ???
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
#Minidao配置
minidao:
  base-package: org.jeecg.modules.jmreport.*,org.jeecg.modules.drag.*
#JimuReport[上传配置]
jeecg:
  # local|minio|alioss
  uploadType: oss
  # local
  path:
    #文件路径
    upload: /opt/upload
  # alioss
  oss:
    accessKey: ??
    secretKey: ??
    endpoint: oss-cn-beijing.aliyuncs.com
    bucketName: jeecgdev
  # minio
  minio:
    minio_url: http://minio.jeecg.com
    minio_name: ??
    minio_pass: ??
    bucketName: ??
#输出sql日志
logging:
  level:
    org.jeecg.modules.jmreport: info
